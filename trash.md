 // function checkIfThisChampIsIn(champion?: Champion) {
      //   const champions = enemyChampions.filter(
      //     ({ lane }) => lane !== summonerLane
      //   );

      //   console.log(champions);
      // }

      // checkIfThisChampIsIn(topChamps.find(({ lane }) => lane === summonerLane));
